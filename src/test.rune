pub fn get_query_terms() {
    let terms = [Stretch::term_id()];
    terms
}

pub fn query(q) {
  for items in q {
     match items {
        (stretch) => {
            stretch.x = 0.5;
        }
     }
  }
}