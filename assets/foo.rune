#[system(Update, query: Query<&Stretch>)]
pub fn foo(query) {
    for i in query {
        match i {
            (stretch) => {
                if stretch.x > 3.0 {
                    //stretch.x = 0.001;
                }
                //stretch.x += 0.1;
            }
        }
    }
}